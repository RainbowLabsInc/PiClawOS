# Generated Directory

The `piclaw/` subdirectory in this folder is **not tracked by git**.

It is generated automatically by running:

```bash
make sync
```

from the `piclaw-os/` root. This copies the main source from `piclaw-os/piclaw/`
into this directory for offline SD card installations.

For online installations, the installer clones directly from GitHub.
